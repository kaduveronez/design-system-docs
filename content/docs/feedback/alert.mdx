---
title: Alert
description: Notificações e alertas inline para o usuário.
---

# Alert

Exibe mensagens importantes em destaque na interface.

import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert'
import { Sparkles, Terminal } from 'lucide-react'
import { ComponentPreview } from '@/components/docs/ComponentPreview'

## Preview Interativo

<ComponentPreview code={`import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"\nimport { Terminal } from "lucide-react"\n\nexport function AlertDemo() {\n  return (\n    <Alert>\n      <Terminal className="h-4 w-4" />\n      <AlertTitle>Atualização Disponível!</AlertTitle>\n      <AlertDescription>\n        O sistema Gen QRcode v2.0 foi publicado.\n      </AlertDescription>\n    </Alert>\n  )\n}`}>
  <div className="w-full max-w-md space-y-4">
    <Alert>
      <Terminal className="h-4 w-4 text-primary" />
      <AlertTitle className="font-semibold">Atualização Disponível!</AlertTitle>
      <AlertDescription className="text-xs text-muted-foreground">
        O motor de geração de QR Codes v2.0 foi ativado.
      </AlertDescription>
    </Alert>

    <Alert variant="destructive">
      <AlertTitle className="font-semibold">Atenção ao Link</AlertTitle>
      <AlertDescription className="text-xs">
        A URL de destino informada não respondeu com código HTTP 200 OK.
      </AlertDescription>
    </Alert>
  </div>
</ComponentPreview>

## Código Manual

```tsx
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert"

export function AlertDemo() {
  return (
    <Alert>
      <AlertTitle>Aviso</AlertTitle>
      <AlertDescription>Descrição do alerta</AlertDescription>
    </Alert>
  )
}
```
