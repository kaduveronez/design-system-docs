---
title: Dropdown Menu
description: Menu suspenso para exibição de ações secundárias.
---

# Dropdown Menu

Exibe um menu suspenso de ações em resposta ao clique de um gatilho.

import { DropdownMenuDemo } from '@/components/docs/demos/DropdownMenuDemo'
import { ComponentPreview } from '@/components/docs/ComponentPreview'

## Preview Interativo

Clique no botão abaixo para abrir o menu suspenso interativo real:

<ComponentPreview code={`import {\n  DropdownMenu,\n  DropdownMenuContent,\n  DropdownMenuItem,\n  DropdownMenuLabel,\n  DropdownMenuSeparator,\n  DropdownMenuTrigger,\n} from "@/components/ui/dropdown-menu"\nimport { Button } from "@/components/ui/button"\n\nexport function DropdownMenuDemo() {\n  return (\n    <DropdownMenu>\n      <DropdownMenuTrigger render={<Button variant="outline">Minha Conta</Button>} />\n      <DropdownMenuContent className="w-56">\n        <DropdownMenuLabel>Minha Conta</DropdownMenuLabel>\n        <DropdownMenuSeparator />\n        <DropdownMenuItem>Gerenciar Perfil</DropdownMenuItem>\n        <DropdownMenuItem>Meus QR Codes</DropdownMenuItem>\n      </DropdownMenuContent>\n    </DropdownMenu>\n  )\n}`}>
  <DropdownMenuDemo />
</ComponentPreview>

## Código Manual

```tsx
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuLabel,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu"
import { Button } from "@/components/ui/button"

export function DropdownDemo() {
  return (
    <DropdownMenu>
      <DropdownMenuTrigger render={<Button variant="outline">Abrir Menu</Button>} />
      <DropdownMenuContent>
        <DropdownMenuLabel>Minha Conta</DropdownMenuLabel>
        <DropdownMenuSeparator />
        <DropdownMenuItem>Perfil</DropdownMenuItem>
        <DropdownMenuItem>Configurações</DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  )
}
```
