---
title: Badge
description: Etiquetas de status e marcadores curtos.
---

# Badge

Exibe rótulos visuais curtos para indicar estados ou contagens.

import { Badge } from '@/components/ui/badge'
import { ComponentPreview } from '@/components/docs/ComponentPreview'

## Preview Interativo

<ComponentPreview code={`import { Badge } from "@/components/ui/badge"\n\nexport function BadgeDemo() {\n  return (\n    <div className="flex gap-2">\n      <Badge variant="default">Ativo</Badge>\n      <Badge variant="secondary">Secundário</Badge>\n      <Badge variant="destructive">Erro</Badge>\n      <Badge variant="outline">Contorno</Badge>\n    </div>\n  )\n}`}>
  <div className="flex flex-wrap items-center justify-center gap-3">
    <Badge variant="default" className="bg-primary text-primary-foreground">Ativo</Badge>
    <Badge variant="secondary">Pendente</Badge>
    <Badge variant="destructive">Excluído</Badge>
    <Badge variant="outline">Rascunho</Badge>
  </div>
</ComponentPreview>

## Código Manual

```tsx
import { Badge } from "@/components/ui/badge"

export function BadgeDemo() {
  return <Badge variant="default">Ativo</Badge>
}
```
