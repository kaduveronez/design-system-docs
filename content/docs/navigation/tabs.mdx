---
title: Tabs
description: Abas compactas para organização de conteúdos intercalados.
---

# Tabs

Alterna entre diferentes visões de conteúdo dentro de um mesmo contêiner.

import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'
import { ComponentPreview } from '@/components/docs/ComponentPreview'

## Preview Interativo

<ComponentPreview code={`import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"\n\nexport function TabsDemo() {\n  return (\n    <Tabs defaultValue="url" className="w-[350px]">\n      <TabsList className="grid w-full grid-cols-2 h-9 p-1">\n        <TabsTrigger value="url">Link Web</TabsTrigger>\n        <TabsTrigger value="vcard">Cartão VCard</TabsTrigger>\n      </TabsList>\n      <TabsContent value="url" className="p-4 border rounded-lg mt-2">Conteúdo Link Web</TabsContent>\n      <TabsContent value="vcard" className="p-4 border rounded-lg mt-2">Conteúdo VCard</TabsContent>\n    </Tabs>\n  )\n}`}>
  <Tabs defaultValue="url" className="w-[350px]">
    <TabsList className="grid w-full grid-cols-2 h-9 p-1 bg-muted rounded-lg">
      <TabsTrigger value="url" className="text-xs font-medium py-1">Link Web</TabsTrigger>
      <TabsTrigger value="vcard" className="text-xs font-medium py-1">Cartão VCard</TabsTrigger>
    </TabsList>
    <TabsContent value="url" className="p-4 border border-border rounded-xl bg-card mt-3 text-xs space-y-1">
      <p className="font-semibold text-foreground">Configuração de Link Web</p>
      <p className="text-muted-foreground">Insira a URL de destino para o seu QR Code dinâmico.</p>
    </TabsContent>
    <TabsContent value="vcard" className="p-4 border border-border rounded-xl bg-card mt-3 text-xs space-y-1">
      <p className="font-semibold text-foreground">Cartão VCard Profissional</p>
      <p className="text-muted-foreground">Preencha nome, cargo, telefone e e-mail de contato.</p>
    </TabsContent>
  </Tabs>
</ComponentPreview>

## Código Manual

```tsx
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"

export function ExemploTabs() {
  return (
    <Tabs defaultValue="url">
      <TabsList className="h-9">
        <TabsTrigger value="url">Link Web</TabsTrigger>
        <TabsTrigger value="vcard">VCard</TabsTrigger>
      </TabsList>
    </Tabs>
  )
}
```
