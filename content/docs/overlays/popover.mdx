---
title: Popover
description: Balão com conteúdo rico ancorado a um elemento.
---

# Popover

Exibe conteúdo sobreposto ancorado diretamente a um elemento de gatilho.

import { PopoverDemo } from '@/components/docs/demos/PopoverDemo'
import { ComponentPreview } from '@/components/docs/ComponentPreview'

## Preview Interativo

Clique no botão para ancorar o balão de filtro interativo real:

<ComponentPreview code={`import {\n  Popover,\n  PopoverContent,\n  PopoverTrigger,\n} from "@/components/ui/popover"\nimport { Button } from "@/components/ui/button"\n\nexport function PopoverDemo() {\n  return (\n    <Popover>\n      <PopoverTrigger asChild>\n        <Button variant="outline">Filtros Avançados</Button>\n      </PopoverTrigger>\n      <PopoverContent className="w-80">\n        <div className="space-y-2">\n          <h4 className="font-semibold text-sm">Filtrar QR Codes</h4>\n        </div>\n      </PopoverContent>\n    </Popover>\n  )\n}`}>
  <PopoverDemo />
</ComponentPreview>

## Código Manual

```tsx
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover"
import { Button } from "@/components/ui/button"

export function ExemploPopover() {
  return (
    <Popover>
      <PopoverTrigger asChild><Button variant="outline">Popover</Button></PopoverTrigger>
      <PopoverContent><p className="text-sm">Conteúdo do Popover</p></PopoverContent>
    </Popover>
  )
}
```
