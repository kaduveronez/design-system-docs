---
title: Dialog (Modal)
description: Janela modal sobreposta interativa para formulários e tarefas focadas.
---

# Dialog

Caixa de diálogo modal que se posiciona sobre a página para capturar ações focadas do usuário.

import { DialogDemo } from '@/components/docs/demos/DialogDemo'
import { ComponentPreview } from '@/components/docs/ComponentPreview'

## Preview Interativo

Clique no botão abaixo para abrir a janela modal interativa real:

<ComponentPreview code={`import {\n  Dialog,\n  DialogContent,\n  DialogDescription,\n  DialogHeader,\n  DialogTitle,\n  DialogTrigger,\n} from "@/components/ui/dialog"\nimport { Button } from "@/components/ui/button"\nimport { Input } from "@/components/ui/input"\n\nexport function DialogDemo() {\n  return (\n    <Dialog>\n      <DialogTrigger asChild>\n        <Button variant="default">Criar QR Code</Button>\n      </DialogTrigger>\n      <DialogContent>\n        <DialogHeader>\n          <DialogTitle>Criar QR Code Dinâmico</DialogTitle>\n          <DialogDescription>\n            Digite o destino e o nome da sua nova campanha.\n          </DialogDescription>\n        </DialogHeader>\n        <Input placeholder="https://genqrcode.com/promo" />\n        <Button type="submit">Gerar QR Code</Button>\n      </DialogContent>\n    </Dialog>\n  )\n}`}>
  <DialogDemo />
</ComponentPreview>

## Código Manual

```tsx
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog"
import { Button } from "@/components/ui/button"

export function DialogDemo() {
  return (
    <Dialog>
      <DialogTrigger asChild><Button>Abrir Modal</Button></DialogTrigger>
      <DialogContent>
        <DialogHeader><DialogTitle>Título Modal</DialogTitle></DialogHeader>
      </DialogContent>
    </Dialog>
  )
}
```
