---
title: Sheet
description: Painel lateral expansível e interativo para formulários e configurações.
---

# Sheet

Painel lateral (Drawer/Offcanvas) que desliza suavemente a partir da borda da tela.

import { SheetDemo } from '@/components/docs/demos/SheetDemo'
import { ComponentPreview } from '@/components/docs/ComponentPreview'

## Preview Interativo

Clique no botão abaixo para deslizar o painel lateral de configurações:

<ComponentPreview code={`import {\n  Sheet,\n  SheetContent,\n  SheetDescription,\n  SheetHeader,\n  SheetTitle,\n  SheetTrigger,\n} from "@/components/ui/sheet"\nimport { Button } from "@/components/ui/button"\n\nexport function SheetDemo() {\n  return (\n    <Sheet>\n      <SheetTrigger asChild>\n        <Button variant="outline">Configurações</Button>\n      </SheetTrigger>\n      <SheetContent>\n        <SheetHeader>\n          <SheetTitle>Estilização do QR Code</SheetTitle>\n          <SheetDescription>\n            Personalize as propriedades visuais da imagem gerada.\n          </SheetDescription>\n        </SheetHeader>\n      </SheetContent>\n    </Sheet>\n  )\n}`}>
  <SheetDemo />
</ComponentPreview>

## Código Manual

```tsx
import { Sheet, SheetContent, SheetHeader, SheetTitle, SheetTrigger } from "@/components/ui/sheet"
import { Button } from "@/components/ui/button"

export function ExemploSheet() {
  return (
    <Sheet>
      <SheetTrigger asChild><Button variant="outline">Painel</Button></SheetTrigger>
      <SheetContent>
        <SheetHeader><SheetTitle>Configurações</SheetTitle></SheetHeader>
      </SheetContent>
    </Sheet>
  )
}
```
