---
title: Form
description: Estrutura de validação e controle de formulários com React Hook Form e Zod.
---

# Form

Encapsulador de formulários com validação e tratamento de erros integrado.

import { Input } from '@/components/ui/input'
import { Button } from '@/components/ui/button'
import { ComponentPreview } from '@/components/docs/ComponentPreview'

## Preview Interativo

<ComponentPreview code={`import { Input } from "@/components/ui/input"\nimport { Button } from "@/components/ui/button"\n\nexport function FormDemo() {\n  return (\n    <form className="space-y-4 max-w-sm w-full">\n      <Input placeholder="Nome da campanha" />\n      <Input placeholder="URL de destino" />\n      <Button type="button" className="w-full">Gerar QR Code</Button>\n    </form>\n  )\n}`}>
  <div className="space-y-4 max-w-sm w-full">
    <div className="space-y-1">
      <label className="text-xs font-medium text-muted-foreground">Nome da Campanha</label>
      <Input placeholder="Campanha Verão 2026" />
    </div>
    <div className="space-y-1">
      <label className="text-xs font-medium text-muted-foreground">URL do Destino</label>
      <Input placeholder="https://genqrcode.com/promo" />
    </div>
    <Button type="button" className="w-full bg-primary text-primary-foreground">Criar QR Code</Button>
  </div>
</ComponentPreview>

## Código Manual

```tsx
import { Input } from "@/components/ui/input"
import { Button } from "@/components/ui/button"

export function FormDemo() {
  return (
    <form className="space-y-4">
      <Input placeholder="URL" />
      <Button type="submit">Gerar</Button>
    </form>
  )
}
```
